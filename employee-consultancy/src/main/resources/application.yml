spring:
  datasource:
    url: jdbc:mysql://localhost:3306/job_consultancy
    username: root
    password: Root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update


server:
  port: 8082

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
